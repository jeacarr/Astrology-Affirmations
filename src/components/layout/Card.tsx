import { TarotCard } from "@/state/data/tarotCard/TarotCard";

interface Props{
    reading: TarotCard;
    upright: boolean;
    image: string;
}

const Card: React.FC<Props> = (props: Props) => {
    return (
        <>
        
        </>
    )
}